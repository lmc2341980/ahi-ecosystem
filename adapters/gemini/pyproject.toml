[project]
name = "ahi-adapter-gemini"
version = "0.1.0"
description = "Google Gemini adapter for AHI"
requires-python = ">=3.11"

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]
